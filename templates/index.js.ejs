const express = require('express');
let router = express.Router();

router.get('/', (req, res) =>{
<% if (view) { -%>
    res.render('index', { title: 'Express Generator' })
<% } else { -%>
    res.send('GET Request on Index')
<% } -%>
})

module.exports = router